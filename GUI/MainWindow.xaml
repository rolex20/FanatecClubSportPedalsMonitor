<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PedDash.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PedDash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PedDash">
    <Grid Background="#050508">
        <Grid.Resources>
            <Style x:Key="NavButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="#e0e0e0" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="BorderThickness" Value="0,0,0,2" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Padding" Value="15,12" />
                <Setter Property="CornerRadius" Value="0" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#333" BorderThickness="0,0,0,1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#1a1a24" Offset="0" />
                    <GradientStop Color="#0f0f16" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid Padding="20,15,30,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                    <TextBlock Text="PedDash" Foreground="#00f3ff" FontWeight="Bold" FontSize="20" />
                    <TextBlock Text="Telemetry Arcade" Foreground="#777" FontSize="12" Margin="10,6,0,0" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" Spacing="10">
                    <Border x:Name="DiscPill" Background="#1A1100" BorderBrush="#ffbf00" BorderThickness="1" CornerRadius="12" Padding="12,4" Visibility="Collapsed">
                        <TextBlock Text="DISCONNECTED" Foreground="#ffbf00" FontSize="12" FontWeight="Bold" />
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Column="2">
                    <Border Tapped="Latency_Tapped" Background="Transparent" HorizontalAlignment="Right">
                        <FlyoutBase.AttachedFlyout>
                            <Flyout>
                                <StackPanel MaxWidth="300" Spacing="10">
                                    <TextBlock Text="Total Latency" FontWeight="Bold" Foreground="#00f3ff" />
                                    <TextBlock Text="Total time from pedal sample to the next painted WinUI frame. This is your true perceived latency." TextWrapping="Wrap" Foreground="#ccc" />
                                </StackPanel>
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="24" FontWeight="Bold" Foreground="#e0e0e0" FontFamily="Consolas" Text="Latency:" />
                            <TextBlock x:Name="RunLagTotal" MinWidth="96" TextAlignment="Right" FontSize="24" FontWeight="Bold" Foreground="#ff3333" FontFamily="Consolas" Text="-- ms" Margin="4,0,0,0" />
                        </StackPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,2,0,0">
                        <Border Tapped="Latency_Tapped" Background="Transparent">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout>
                                    <StackPanel MaxWidth="300" Spacing="10">
                                        <TextBlock Text="Tick Period" FontWeight="Bold" Foreground="#00f3ff" />
                                        <TextBlock Text="Time since the previous runtime tick completed and the next sample loop began." TextWrapping="Wrap" Foreground="#ccc" />
                                    </StackPanel>
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="14" Foreground="#777" FontFamily="Consolas" Text="Tick: " />
                                <TextBlock x:Name="RunTickPeriod" MinWidth="54" TextAlignment="Right" FontSize="14" Foreground="#777" FontFamily="Consolas" Text="00"  />
                            </StackPanel>
                        </Border>

                        <TextBlock FontSize="14" Foreground="#777" FontFamily="Consolas" Text=" | " />
                        <Border Tapped="Latency_Tapped" Background="Transparent">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout>
                                    <StackPanel MaxWidth="300" Spacing="10">
                                        <TextBlock Text="Read MS" FontWeight="Bold" Foreground="#00f3ff" />
                                        <TextBlock Text="Time spent actively copying memory from hardware or API." TextWrapping="Wrap" Foreground="#ccc" />
                                    </StackPanel>
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="14" Foreground="#777" FontFamily="Consolas" Text="Read: " />
                                <TextBlock x:Name="RunRead" MinWidth="54" TextAlignment="Right" FontSize="14" Foreground="#777" FontFamily="Consolas" Text="00" />
                            </StackPanel>
                        </Border>

                        <TextBlock FontSize="14" Foreground="#777" FontFamily="Consolas" Text=" | " />
                        <Border Tapped="Latency_Tapped" Background="Transparent">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout>
                                    <StackPanel MaxWidth="300" Spacing="10">
                                        <TextBlock Text="Compute MS" FontWeight="Bold" Foreground="#00f3ff" />
                                        <TextBlock Text="CPU time spent running logic and preparing graphs." TextWrapping="Wrap" Foreground="#ccc" />
                                    </StackPanel>
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="14" Foreground="#777" FontFamily="Consolas" Text="Compute: " />
                                <TextBlock x:Name="RunCompute" MinWidth="54" TextAlignment="Right" FontSize="14" Foreground="#777" FontFamily="Consolas" Text="00" />
                            </StackPanel>
                        </Border>

                        <TextBlock FontSize="14" Foreground="#777" FontFamily="Consolas" Text=" | " />
                        <Border Tapped="Latency_Tapped" Background="Transparent">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout>
                                    <StackPanel MaxWidth="300" Spacing="10">
                                        <TextBlock Text="Paint MS" FontWeight="Bold" Foreground="#00f3ff" />
                                        <TextBlock Text="Time spent rasterizing the Win2D visual lines onto the screen." TextWrapping="Wrap" Foreground="#ccc" />
                                    </StackPanel>
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="14" Foreground="#777" FontFamily="Consolas" Text="Paint: " />
                                <TextBlock x:Name="RunPaint" MinWidth="54" TextAlignment="Right" FontSize="14" Foreground="#777" FontFamily="Consolas" Text="00" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#0f0f16" BorderBrush="#222" BorderThickness="0,0,0,1">
            <StackPanel x:Name="NavPanel" Orientation="Horizontal" Spacing="0" Margin="20,0,0,0">
                <Button x:Name="NavRacing" Content="Racing" CommandParameter="Racing" Style="{StaticResource NavButtonStyle}" Click="NavButton_Click" />
                <Button Content="Lag &amp; Timing" CommandParameter="Lag" Style="{StaticResource NavButtonStyle}" Click="NavButton_Click" />
                <Button Content="Signals &amp; Events" CommandParameter="Signals" Style="{StaticResource NavButtonStyle}" Click="NavButton_Click" />
                <Button Content="Data Map" CommandParameter="DataMap" Style="{StaticResource NavButtonStyle}" Click="NavButton_Click" />
                <Button Content="Configuration" CommandParameter="Config" Style="{StaticResource NavButtonStyle}" Click="NavButton_Click" />
            </StackPanel>
        </Border>

        <Frame x:Name="ContentFrame" Grid.Row="2" Padding="15" />
    </Grid>
</Window>
